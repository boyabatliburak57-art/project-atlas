# Mobile Portfolio Performance

The native chart renders backend-authoritative portfolio value, invested capital and optional benchmark series. External deposits and withdrawals are excluded from investment return. Missing days are not interpolated. Benchmark comparison requires aligned range, base date, calendar and currency; otherwise it reports `BENCHMARK_UNAVAILABLE`. Client code does not implement return formulas.
