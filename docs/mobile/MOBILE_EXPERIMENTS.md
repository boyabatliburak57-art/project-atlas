# Mobile Experiments

Experiments bind immutable strategy revisions to bounded parameter sets. Mobile limits the UX to 64 combinations and the backend independently validates authorization, complexity, rate and concurrency limits. Comparisons are neutral tables with user-selected sorting and do not rank or recommend a strategy.

Duplicate and re-run create new resources and do not overwrite history. Same-configuration/current-data and exact-historical-reproduction are separate actions.
