# Mobile Portfolio Privacy

Privacy mode masks portfolio value, P/L and position amounts visually and in the accessibility tree. Financial values, names, notes and resource identifiers are excluded from telemetry, push and default share payloads. Cached private data remains ownership-scoped and is cleared on logout/user switch. Test fixtures are compile-time replaced by an empty production module.
