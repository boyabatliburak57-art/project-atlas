# Mobile Portfolio Risk

Risk cards expose methodology version, period, observations, cutoff and availability. Concentration, volatility and drawdown use backend snapshots. Benchmark-relative metrics require an available benchmark. Insufficient data is `NOT_EVALUABLE`, never zero. VaR is shown only when the backend capability exists and is never described as a guaranteed maximum loss.
