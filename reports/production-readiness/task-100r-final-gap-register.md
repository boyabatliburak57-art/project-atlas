# TASK-100R Final Gap Register

| ID | Severity | Classification | Gap | Evidence | Minimal remediation | Required re-test |
| --- | --- | --- | --- | --- | --- | --- |
| R-GAP-001 | HIGH | NON_STAGING_BLOCKER | Production Docker targets do not build the current 14-workspace graph. | `docker build --target web` fails because workspace manifests/dependencies for `design-tokens`, `financial-formatting` and other mobile-era packages are absent from the install layer; API/worker/migration images therefore have no current proof either. | Update the Docker manifest/install/pruning strategy for every current workspace without weakening tests. | Build web/API/worker/migration targets; run container smoke, non-root/health checks and image scans. |
| R-GAP-002 | HIGH | NON_STAGING_BLOCKER | Production dependency policy fails. | `pnpm audit --prod --audit-level high`: critical 0, high 7, moderate 4, low 1. High findings include `brace-expansion`, `fast-uri`, `image-size`, `js-yaml`, and `nanoid`; no approved risk exception exists. | Upgrade/override safely or document an approved, time-bounded security exception with reachability analysis. | Frozen install, full tests/builds, dependency audit, production bundle and container scans. |
| R-GAP-003 | HIGH | NON_STAGING_BLOCKER | Production license policy fails. | `pnpm license:check` inventories 712 packages and rejects eight unreviewed expressions, including `MPL-2.0` and `(BSD-3-Clause OR GPL-2.0)`. | Legal/security review the expressions and encode explicit allowed/denied decisions; replace dependencies where necessary. | License gate, SBOM license inventory and release checklist. |
| R-GAP-004 | HIGH | NON_STAGING_BLOCKER | No current-candidate SBOM or complete source/container provenance exists. | Historical SBOMs target older RCs. Current container build fails; registry/store artifacts were not produced. | Resolve R-GAP-001, generate commit-bound source/build SBOM, and let CI emit provenance/attestations; retain registry/store provenance as an external artifact gate. | SBOM validation, provenance verification, digest binding and container scan. |
| R-GAP-005 | HIGH | NON_STAGING_BLOCKER | The audited candidate is not immutable. | HEAD is `fac5bfe...`, but runtime changes are uncommitted/untracked. The source fingerprint identifies bytes for audit only and is not a commit or registry artifact. | Review and commit intended changes, remove unintended artifacts, obtain a clean tree, and assign an RC version/build identity. | Re-run every current-commit gate; verify clean tree, SHA, source fingerprint and artifact digests. |
| R-GAP-006 | LOW | NON_BLOCKING | Repository contains tracked OS metadata. | `.DS_Store` and `docs/.DS_Store` are tracked; root also contains a large mockup asset requiring ownership confirmation. | Remove OS metadata through normal review and confirm the mockup is intentional. | Hygiene scan and `git diff --check`. |

Non-staging blockers: **5**. External credentials/deployments are tracked in the prerequisite matrix
and do not replace or excuse these internal blockers.
